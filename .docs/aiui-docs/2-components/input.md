# Input 输入框

`input` 组件用于接收单行文本内容，适合搜索、表单填写和简短信息录入场景。

## 使用方法

```xml
<input
  value="{{keyword}}"
  placeholder="请输入搜索内容"
  bindinput="handleInput"
/>
```

## 功能特性

- 支持单行文本输入和实时内容更新。
- 支持占位提示文本。
- 适合搜索框、登录表单和基础信息录入场景。
