# Image

The `image` component is used to display images.

## Usage

```xml
<image src="path/to/image.png" class="logo"></image>
```

## Features

- Supports local and network images (planned).
- Supports multiple scaling modes, such as aspect fit for proportional scaling and aspect fill for proportional filling.
