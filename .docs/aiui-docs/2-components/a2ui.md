# A2UI AI 组件

`a2ui` 组件用于承载 AI 交互场景中的原生 UI 能力，适合语音、多轮对话和智能任务流等界面构建。

## 使用方法

```xml
<a2ui
  agent-id="assistant"
  session-id="{{sessionId}}"
  bindmessage="handleMessage"
></a2ui>
```

## 功能特性

- 面向 AI 对话和任务执行流程提供交互容器。
- 适合承载消息流、状态反馈和智能操作入口。
- 可与业务页面组合，构建原生 AI 应用体验。
