# 多媒体 (media)

提供与设备媒体（如摄像头、录音机、图片等）交互的接口。

## 相机

- `wx.createCameraContext()`: 创建 camera 上下文 CameraContext 对象。

详见：[相机](/AIUI/api/media-camera)

## 录音

- `wx.getRecorderManager()`: 获取全局唯一的录音管理器 RecorderManager。

详见：[录音](/AIUI/api/media-recorder)
