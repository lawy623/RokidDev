# Source Map

The AIUI compiler automatically generates source map files. In DevTools, you can inspect the unobfuscated original code, which makes it easier to locate the line numbers where errors occur.
