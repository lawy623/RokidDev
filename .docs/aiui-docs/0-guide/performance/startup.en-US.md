# Startup Performance

- **Code package size**: Keep the package as small as possible and remove unused resources.
- **Subpackage loading**: Split the agent into multiple subpackages and load them on demand.
- **Prefetching**: Prefetch core data before startup.
